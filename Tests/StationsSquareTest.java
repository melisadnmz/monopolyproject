import org.junit.Test;

import static org.junit.Assert.*;

public class StationsSquareTest {

    @Test
    public void testGetSquare() {
        /*
        Board board = new Board();
        for (int i = 0; i < 40; i++) {
            System.out.println(board.getSquare()[i].getName());
            if (i == 10)
                assertEquals(board.getSquare()[i].getName().equals("JailSquare"),"JailSquare");
            else if (i == 0)
                assertEquals(board.getSquare()[i].getName().equals("GO"),"GO");
            else if (i == 20)
                assertEquals(board.getSquare()[i].getName().equals("FreeParking"),"FreeParking");
            else if (i == 30)
                assertEquals(board.getSquare()[i].getName().equals("GoToJailSquare"),"GoToJailSquare");
            else if (i == 38)
                assertEquals(board.getSquare()[i].getName().equals("Tax"),"Tax");
            else
                continue;
        }

         */
    }
}